name: clip-mania
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - cudatoolkit
  - pip
  - pip:
      - numpy
      - pandas
      - torch~=1.7.1
      - torchvision~=0.8.2
      - scikit-image
      - ftfy
      - regex
      - tqdm
      - pillow
      - pytest
      - absl-py
      - git+https://github.com/openai/CLIP.git